# Foodie Backend Environment Configuration

# ==========================================
# Server Configuration
# ==========================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# ==========================================
# PostgreSQL Database Configuration
# ==========================================
# PostgreSQL container configuration (for docker-compose)
POSTGRES_USER=root
POSTGRES_PASSWORD=123456789@
POSTGRES_DB=foodie
POSTGRES_PORT=5432

# Connection string will be auto-built from POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB above
# If you want to override, uncomment and set SQL_DSN:
# SQL_DSN=postgres://root:123456789@@localhost:5432/foodie?sslmode=disable

# ==========================================
# Redis Cache Configuration
# ==========================================
# Cache type: "redis" or "memory" (default: "memory")
CACHE_TYPE=memory

# Redis Configuration (required if CACHE_TYPE=redis)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# ==========================================
# Message Broker Configuration
# ==========================================
# Message broker type: "rabbitmq" or "memory" (default: "memory")
MESSAGE_BROKER_TYPE=memory

# RabbitMQ Configuration (required if MESSAGE_BROKER_TYPE=rabbitmq)
RABBITMQ_URL=amqp://admin:admin123@localhost:5672/
RABBITMQ_EXCHANGE=foodie_events

# ==========================================
# Logging Configuration
# ==========================================
LOG_LEVEL=info
LOG_FORMAT=json
LOG_OUTPUT=stdout

# ==========================================
# Swagger/API Documentation
# ==========================================
ENABLE_SWAGGER=true
SWAGGER_PATH=/swagger

# Swagger/OpenAPI Documentation
ENABLE_SWAGGER=true
SWAGGER_PATH=/swagger
# Optional: Set username and password to enable Basic Auth for Swagger UI
# Leave empty to disable authentication (development only)
SWAGGER_USERNAME=root
SWAGGER_PASSWORD=123456789@