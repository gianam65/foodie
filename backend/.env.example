# Foodie Backend Environment Configuration
# Copy this file to .env and customize for your environment

# ==========================================
# Server Configuration
# ==========================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# ==========================================
# Database Configuration (PostgreSQL/MySQL)
# ==========================================
# SQL Database Connection String
# PostgreSQL example:
SQL_DSN=postgres://user:password@localhost:5432/foodie?sslmode=disable
# MySQL example:
# SQL_DSN=mysql://user:password@tcp(localhost:3306)/foodie

# ==========================================
# Redis Cache Configuration
# ==========================================
# Cache type: "redis" or "memory" (default: "memory")
# - "memory": In-memory cache (for development/testing, no Redis needed)
# - "redis": Redis cache (for production)
CACHE_TYPE=memory

# Redis Configuration (required if CACHE_TYPE=redis)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# ==========================================
# Message Broker Configuration (RabbitMQ)
# ==========================================
# Message broker type: "rabbitmq", "memory", or "kafka" (default: "memory")
# - "memory": In-memory publisher (for development/testing, no broker needed)
# - "rabbitmq": RabbitMQ message broker (for production)
MESSAGE_BROKER_TYPE=memory

# RabbitMQ Configuration (required if MESSAGE_BROKER_TYPE=rabbitmq)
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
RABBITMQ_EXCHANGE=foodie_events

# ==========================================
# Optional: Logging Configuration
# ==========================================
# LOG_LEVEL=info
# LOG_FORMAT=json

# ==========================================
# Optional: Feature Flags
# ==========================================
# ENABLE_METRICS=true
# ENABLE_TRACING=false

# ==========================================
# Logging Configuration
# ==========================================
# Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# Log format: json, text (default: json)
# Use 'json' for Grafana/Loki integration
LOG_FORMAT=json

# Log output: stdout, stderr, or file path (default: stdout)
# Use 'stdout' for Docker/Kubernetes log aggregation
LOG_OUTPUT=stdout

# ==========================================
# Swagger/API Documentation Configuration
# ==========================================
# Enable Swagger UI: true, false (default: true)
# Set to false in production for security
ENABLE_SWAGGER=true

# Swagger UI path (default: /swagger/)
# SWAGGER_PATH=/swagger/
